apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: ecommerce

spec:
  selector:
    app: order-service

  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30082

  type: NodePort
